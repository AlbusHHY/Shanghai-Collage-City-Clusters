{
  "name": "shanghai-collage-city-cluster-votes",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:schema": "wrangler d1 execute shanghai-collage-city-cluster-votes --remote --file=schema.sql",
    "db:seed": "wrangler d1 execute shanghai-collage-city-cluster-votes --remote --file=seed.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}

